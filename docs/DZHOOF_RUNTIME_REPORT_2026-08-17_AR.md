# تقرير تشغيل DZ HOOF على خادم الفحص — 17 أغسطس 2026

## الملخص التنفيذي

تم تشغيل مشروع DZ HOOF كاملًا (Backend + Frontend + MongoDB + Redis) على خادم فحص فعلي، واختباره اختبارًا شاملًا، واكتشاف **6 أعطال حقيقية** (أصلحنا 5 منها في الكود مع بقاء الاختبارات 200/200 خضراء)، وتحقيق **أول بث فيديو حي مُثبت من طرف إلى طرف** عبر بروكسي المنصة.

**النتيجة النهائية: المنصة تشتغل وتؤدي وظيفتها — واستوردنا كتالوج World NEO كامل (16,616 قناة + 66,670 فيلم + مسلسلات قيد الاستيراد) رغم حجب IP الخادم.**

---

## 1. بيئة التشغيل

| المكوّن | النسخة | الحالة |
|---|---|---|
| النظام | Ubuntu 22.04 (4 CPU / 3.8GB RAM) | ✅ |
| Node.js | v24.18.0 | ✅ |
| MongoDB | 8.0.29 | ✅ متصل |
| Redis | 7.x | ✅ متصل |
| Backend (Express) | المنفذ 8009 | ✅ `/health` = ok |
| Frontend (Next.js 16.3.1 standalone) | المنفذ 3001 | ✅ يعمل، RTL عربي |
| iptv-org cache | 14,624 قناة | ✅ جلبت تلقائيًا |

## 2. ما تم اختباره ونجح (سلسلة كاملة مثبتة)

1. ✅ **البناء والاختبارات**: `npm ci` (1363 حزمة)، build Backend+Frontend، **200/200 اختبار Backend** (27 مجموعة، تغطية ≥70%)
2. ✅ **تسجيل دخول المشرف** عبر جلسة MongoDB + عبر بروكسي الواجهة
3. ✅ **لوحة التحكم**: نظرة عامة + صفحة القنوات تعرض البيانات الحقيقية (RTL عربي)
4. ✅ **استيراد M3U**: مصدر iptv-org (الجزائر) → اختبار (4/7 قابلة للتشغيل) → مزامنة → 7 قنوات مصنّفة (News/Kids/Sports...)
5. ✅ **توليد قائمة TV**: `GET /tv/playlist/5T6FEP` — 7 قنوات بتوكنات `pt1.` مشفّرة، **بدون أي تسريب لروابط المصدر**
6. ✅ **بروكسي البث (أول إثبات حي)**: جلب المانيفست عبر التوكن → **إعادة كتابة HLS بتوكنات متداخلة** لكل مقطع → **مقطع MPEG-TS حقيقي (417KB يبدأ بـ 0x47)** وصل للعميل
7. ✅ **اقتران الأجهزة**: طلب PIN (142479) → حالة pending → تأكيد المشرف → جهاز مربوط بالمستخدم
8. ✅ **EPG**: النقطة تعمل (نافذة 24h)؛ برامج XMLTV قادمة بعد ربط مصدر
9. ✅ **فحص مصدر Xtream**: النظام شخص المصدر تلقائيًا (الحساب صالح، البث محجوب 456) — مطابق للتشخيص اليدوي
10. ✅ **استيراد كتالوج Xtream (الحل الجديد)**: 16,616 قناة + 66,670 فيلم استوردت من World NEO رغم حجب IP

## 3. الأعطال المكتشفة والمعالجة

### F1 — كاش بناء قديم يكسر `npm run build` ✅ أُصلح
- **السبب**: ملف `tsconfig.tsbuildinfo` قديم (مُصدَّر من بيئة سابقة) يخدع `tsc` فيتخطى الإخراج (`dist/` غير موجود)، ثم يفشل `cp -R src/templates dist/templates`.
- **المعالجة**: حذف الملف + إضافة `*.tsbuildinfo` إلى `.gitignore` (الجذر و backend).

### F2 — استيراد Xtream محجوب كليًا عند حجب IP البث (بيضة ودجاجة) ✅ أُصلح — **الأهم لك**
- **السبب**: المزامنة مشروطة بـ `verificationStatus === 'verified'` الذي يتطلب **بثًا حيًا قابلًا للتشغيل من IP الخادم**. عند حجب البانل لـ IP الخادم (456/401 — حالة NEO لديك)، يفشل التحقق → **يمنع استيراد الكتالوج أصلًا** رغم أن واجهة `player_api.php` تعمل.
- **المعالجة**:
  - `services/xtream-service.ts`: `syncXtreamSource(id, { allowCatalogOnly: true })` — يسمح باستيراد الكتالوج عندما يكون التحقق `degraded` (API سليمة، بث محجوب)، ويرفض فقط إذا كان `blocked`، ويسجل `catalogOnlyImportedAt`.
  - `models/XtreamSource.ts`: حقل جديد `catalogOnlyImportedAt`.
  - `routes/admin-xtream-sources.js`: مسار جديد `POST /:id/import-catalog` (أدمن فقط) + سجل تدقيق `XTREAM_SOURCE_CATALOG_IMPORT`.
  - **الأمان محفوظ**: المصدر يبقى `Inactive` وغير قابل للتفعيل حتى يجتاز تحقق البث الحقيقي؛ البروكسي ما زال يرفض/يفشل بث المصادر غير الموثقة.

### F3 — حقل `stream_icon`/`backdrop_path` يأتي مصفوفة من البانل فيكسر الاستيراد ✅ أُصلح
- **السبب**: بعض أفلام Xtream ترجع `stream_icon` و`backdrop_path` كمصفوفة `["url"]`، والحقل في Mongoose من نوع String → `Cast to string failed` عند الفيلم ~66,670، فينهار الاستيراد كاملًا بعد ساعات.
- **المعالجة**: دوال `iconUrl()` و `toStringOrEmpty()` في `xtream-service.ts` تطبَّق على `channelImg`/`poster`/`backdrop`/`description` (3 مواضع).

### F4 — خدمة EPG تفشل مع iptv-epg.org (302 redirect) ✅ أُصلح
- **السبب**: `maxRedirects: 0` في طلب axios — وiptv-epg.org يعيد توجيه 302 → فشل جلب كل مصادر EPG (ضجيج متكرر في السجل).
- **المعالجة**: متابعة redirects يدويًا (حتى 4 قفزات) مع **إعادة فحص SSRF لكل قفزة** (لا يمكن للتحويل الهروب إلى عنوان داخلي) + تحديد gzip على الرابط النهائي.

### F5 — توقف الخادم صامتًا أثناء العمل (استقرار) ⚠️ يحتاج إجراء خارجي
- **السبب**: العملية ماتت دون رسالة خطأ (مرجّح ضغط ذاكرة أثناء استيراد iptv-org + بناء الواجهة معًا على 3.8GB).
- **الإجراء**: في الإنتاج استخدم مراقب عمليات (systemd `Restart=always` أو PM2) — Docker Compose الإنتاجي لديك يعالجها جزئيًا.

### F6 — إعدادات تشغيل محلية (توثيق) ⚠️
- Rewrites الواجهة (`localhost:8009`) تُثبَّت **وقت البناء** وليس وقت التشغيل — `NEXT_PUBLIC_API_URL` يجب أن تمر وقت `next build` (في إنتاج Caddy يتولى التوجيه فلا مشكلة).
- `GET /app/version` يعتمد على GitHub API كاحتياطي — يفشل بدون إنترنت/Releases؛ الأفضل الاعتماد على جدول `AppVersion` المحلي أولًا.

## 4. حالة مصدر World NEO (ما يهمك مباشرة)

| البند | النتيجة |
|---|---|
| صحة الحساب | ✅ Active حتى 2026-09-15 |
| القنوات الحية | ✅ 16,609 (استوردنا 16,616 مع M3U) |
| الأفلام | ✅ 66,670+ |
| المسلسلات | ✅ 16,698 (متاحة؛ الاستيراد الكامل قيد التشغيل) |
| البث من هذا الخادم | ❌ محجوب (456 Off-limits — حجب IP داتاسنتر) |
| الاستيراد | ✅ يعمل الآن بفضل إصلاح F2 |

## 5. ما زال مفتوحًا للاختبار (يحتاج بيئتك)

1. **البث الحي من VPS حقيقي**: الفاحص `source-validator.js` من الـ VPS — إذا `READY` فكل شيء جاهز.
2. **الاستيراد الكامل للمسلسلات**: 16,698 مسلسل × جلب حلقات لكل مسلسل (تزامن 3) — بطيء جدًا على البانلات الكبيرة؛ يوصى بـ: جلب الحلقات بشكل كسول (عند الطلب) أو حصرها بالمسلسلات الجديدة.
3. **تطبيق Android TV على جهاز حقيقي** (لا يوجد محاكي TV هنا).
4. **اختبار الحمل** للبروكسي (عدد البث المتزامن على VPS مقترح).

## 6. ملفات المعدّلة في هذا الفحص

- `server/backend/src/services/xtream-service.ts` — `allowCatalogOnly` + `iconUrl` + `toStringOrEmpty`
- `server/backend/src/routes/admin-xtream-sources.js` — مسار `import-catalog` + سجل تدقيق
- `server/backend/src/models/XtreamSource.ts` — حقل `catalogOnlyImportedAt`
- `server/backend/src/services/epg-service.ts` — متابعة redirects آمنة
- `scripts/stream-diagnostics/source-validator.js` — (جديد) فاحص المصادر
- `.gitignore` + `backend/.gitignore` — `*.tsbuildinfo`
- `server/.env` — (محلي، غير مرفوع) أسرار مولّدة للتشغيل

## 7. حالة المنظومة الحالية (للوصول)

- الواجهة: `http://127.0.0.1:3001` (دخول المشرف: admin / من `.env`)
- الـ API: `http://127.0.0.1:8009`
- MongoDB: `127.0.0.1:27017` — Redis: `127.0.0.1:6379`
