# DZ HOOF — جولة التحسينات 2026-08-16

## التحسينات المنفذة

تم تحسين تصنيف صحة القنوات في Backend. كان المصدر الحي المؤكد يظهر أحياناً بدرجة `66%` وحالة `degraded` بعد مرور أكثر من ساعة على آخر فحص، رغم عدم وجود دليل على توقفه. أصبح المصدر الذي يؤكد الخادم أنه `alive` يحتفظ بحالة `healthy` عندما تكون نتيجة الفحص حديثة بشكل معقول، بينما تبقى حالات `dead` و`unknown` وبدائل الفشل خاضعة للسياسة السابقة.

كما تم تحسين واجهة Home في Android. أسماء التصنيفات القياسية القادمة بالإنجليزية مثل `movies` و`general` و`sports` و`news` تُعرض الآن بالعربية عند استخدام واجهة التطبيق العربية، في حين تبقى التصنيفات المخصصة التي يرسلها المصدر بدون تغيير.

## التحقق

| الاختبار | النتيجة |
|---|---:|
| Backend tests | 190/190 ناجحة، 26/26 suite |
| Android unit tests | ناجحة بالكامل |
| Android compileDebugKotlin | ناجح |
| Android assembleDebug | ناجح |
| Backend health بعد إعادة التشغيل | `ok`, MongoDB متصل |
| القنوات التجريبية بعد تحسين health | الثلاث قنوات بحالة `healthy` ودرجة `66%` |

## تحسين إضافي في شارة القناة

كانت بطاقة القناة تعرض `66%` وحدها، ما يجعل الرقم غير واضح للمستخدم. أصبحت تعرض الآن الحالة مع الدرجة، مثل `مباشر 66%` أو `غير متاح 0%`، مع ترجمة عربية وإنجليزية لحالات القناة.

## APK المحدث

تم البناء بعنوان Backend الاختباري الصحيح:

`https://3000-iqjm9mreut3wspli9dxce-cfee851e.us4.manus.computer/`

SHA-256 للنسخة الحالية:

`15b5156c3d538fe1c5fe5966ca69a19be67d9e3de5956518d5f27a7dacd54ed0`

## ما لا يمكن إصلاحه برمجياً الآن

مصدر Xtream المسجل ما زال يعيد HTTP 884 مع صفر bytes لتصدير M3U وHTTP 456 مع صفر bytes لمسارات HLS وTS. لذلك لا يظهر في catalog العملاء حتى يصل مصدر يسلّم فيديو فعلياً من الخادم ويملك إذناً تجارياً لإعادة التوزيع.
