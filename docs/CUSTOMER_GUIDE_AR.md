# دليل زبون DZ HOOF IPTV — التطبيق وكود التفعيل

**تاريخ الإصدار:** 17 أغسطس 2026
**إصدار التطبيق:** 2.0 (versionCode 6) — موقّع رقميًا بشهادة DZ HOOF

---

## 📲 1. ملف التطبيق (APK)

**رابط التنزيل المباشر:**
`https://raising-rolling-webster-residential.trycloudflare.com/downloads/DZHOOF-IPTV-v2.0.apk`

| المعلومة | القيمة |
|---|---|
| الملف | `DZHOOF-IPTV-v2.0.apk` (75.7 MB) |
| اسم الحزمة | `com.dzhoof.iptv` |
| يتطلب | Android 9 (API 28) فأحدث — Android TV / Fire TV / هاتف |
| التوقيع | CN=DZ HOOF — SHA-256: `974e97e09587af6e8cbadaa53cd463282d2ebe1bcd84c4ed7a7fc16f0cf96db1` |
| خادم البث | مرتبط بخادم DZ HOOF (رابط النفق الحالي) |

## 🛠️ 2. طريقة التثبيت

### على Android TV / Fire TV:
1. انسخ ملف `app-release.apk` إلى ذاكرة USB أو حمّله على الجهاز.
2. على التلفاز: افتح **File Manager** (أو استخدم تطبيق مثل "Downloader" وأدخل الرابط) وافتح ملف APK.
3. سماح بتثبيت التطبيقات من مصادر خارجية إذا طُلب (Settings → Security → Unknown sources).
4. ثبّت التطبيق → افتحه.

### على الهاتف (Android):
1. انسخ الملف للهاتف وافتحه من أي مدير ملفات.
2. وافق على "تثبيت من مصادر غير معروفة".

## 🔑 3. كود التفعيل (مشاهدة كل القنوات)

أدخل أحد الأكواد التالية داخل التطبيق (شاشة التفعيل) — كل كود يفتح **جميع القنوات والأفلام والمسلسلات لمدة سنة كاملة** (جهازان):

| # | كود التفعيل |
|---|---|
| 1 | `DZHF-6QWJ-V4CH-KVN9` |
| 2 | `DZHF-SCK4-Y7DV-YURW` |
| 3 | `DZHF-KV5F-9BW7-45BP` |
| 4 | `DZHF-AG7Y-J8NB-EU5P` |
| 5 | `DZHF-YEBV-GFKD-2P34` |

> **ملاحظة مهمة عند الإدخال:** إذا ظهر "Invalid code" فهذا يعني خطأ كتابة بحرف واحد.
> الأزواج الأكثر التباسًا: **S ↔ 5** و **B ↔ 8** و **G ↔ 6** و **V ↔ U** و **Z ↔ 2**.
> أنصح بنسخ الكود ولصقه مباشرة — الشرطات اختيارية (النظام يقبل الكود بدونها) والحروف تكبر تلقائيًا.

> الباقة: **"كل القنوات - سنة كاملة"** — 365 يوم، جهازان، كل الكتالوج (16,616 قناة + 66,670 فيلم + 16,698 مسلسل).

## 📋 4. ماذا بعد التفعيل؟

- الباقة تفعّل **تلقائيًا** على الجهاز: أنشئ حساب زبون + اشتراك فعّال + رمز قائمة قنوات خاص بجهازك.
- افتح التطبيق وستجد: **القنوات الحية** (مصنفة: عربية، فرنسية، رياضة، أطفال...)، **الأفلام**، **المسلسلات**، **البحث**، **المفضلة**، **دليل البرامج EPG**، **التحكم الأبوي**.
- كل قناة تُشغَّل عبر خادم DZ HOOF ببروتوكول مشفّر (لا ترى روابط المصدر أبدًا).

## ⚠️ 5. ملاحظات مهمة (اقرأها)

1. **هذه نسخة تجريبية مرتبطة بنفق مؤقت**: رابط الخادم المدمج في هذا APK هو `https://invitation-permalink-clicking-mate.trycloudflare.com` — يعمل ما دامت جلسة الاختبار حية. **للإصدار النهائي للزبائن، يُبنى APK جديد برابطك الرسمي** (الأمر جاهز أدناه).
2. **كل القنوات تظهر الآن للزبون**: الكتالوج الكامل (16,616 قناة + 66,670 فيلم + 16,698 مسلسل) مفعّل في نسخة التجربة عبر خيار "إظهار المصدر للزبائن" (customerVisible) — تفعيله من لوحة الأدمن → المصادر → تعديل المصدر.
3. **ملاحظة البث من هذا الخادم التجريبي**: قنوات M3U (iptv-org) تشتغل مباشرة. قنوات World NEO تُعرض في القائمة، لكن بثّها الفعلي محجوب من عنوان IP هذا الخادم من جهة المزوّد (456) — **ستشتغل تلقائيًا من خادمك الإنتاجي (VPS)** بعنوان غير محجوب، دون أي تغيير في التطبيق.
4. كل كود تفعيل = جهازان كحد أقصى؛ الجهاز الثالث يرفض تلقائيًا.
5. الاشتراك ينتهي بعد 365 يومًا — عندها يتوقف البث فورًا حتى تفعيل كود جديد.

## 🔧 6. إعادة بناء APK برابطك الرسمي (للمشغّل)

```bash
# على جهاز فيه Java 17 + Android SDK:
cd android
export SIGNING_KEY_STORE=/path/to/dzhoof-release.keystore
export SIGNING_STORE_PASSWORD=...
export SIGNING_KEY_ALIAS=dzhoof
export SIGNING_KEY_PASSWORD=...
DZHOOF_API_URL="https://your-real-domain.com" ./gradlew :app:assembleRelease
# الناتج: app/build/outputs/apk/release/app-release.apk
```

> احتفظ بمفتاح التوقيع (`dzhoof-release.keystore`) في مكان آمن — بدونه لا يمكن تحديث التطبيق عند الزبائن الذين ثبّتوا هذه النسخة.

---
**DZ HOOF IPTV — منصة إدارة وتشغيل القنوات**
